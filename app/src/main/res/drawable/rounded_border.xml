<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- 내부 배경색을 흰색으로 설정 -->
    <corners android:radius="16dp" /> <!-- 모서리를 둥글게 설정 -->
    <stroke
        android:width="0.7dp"
        android:color="@color/gray" /> <!-- 테두리 색상과 두께 설정 -->
</shape>